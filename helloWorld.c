#include <stdio.h>

int main() {
    printf("Hello, World!\n");
    return 0;
}

// To compile the code (before running it), open a terminal / cmd and run:
// (the .c file is saved as "helloWorld.c", so use the right words!)

// **gcc version**
// gcc helloWorld.c -o helloWorld
// Then run: ./helloWorld